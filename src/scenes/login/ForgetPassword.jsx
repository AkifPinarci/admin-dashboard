import React from "react";
import { Box, Typography } from "@mui/material";
const ForgetPassword = () => {
  return (
    <Box>
      <Typography variant="h1">Forget password Page</Typography>
    </Box>
  );
};

export default ForgetPassword;
